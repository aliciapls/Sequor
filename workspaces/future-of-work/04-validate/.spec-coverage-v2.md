# Spec Compliance — A1 (PII-at-rest encryption), shard 1b

Branch `feat/data-layer-security`. Shard 1b = commits `ab7c751..62bf1f2`.
Every row shows the literal verification command and its actual output (no
"exists: yes").

## Spec source: `specs/DEVIATIONS.md` §A1 + `data-model.md` "All PII encrypted at rest (AES-256)"

### Promise 1 — the 9 PII columns are wrapped in `EncryptedString`

```
$ grep -oE 'EncryptedString\(field_name="[^"]+"' src/sequor/db/models.py | grep -E "message_|response_content|learned_|classification_reasoning|escalation_resolution_summary|contact_name"
EncryptedString(field_name="classification_reasoning"
EncryptedString(field_name="contact_name"
EncryptedString(field_name="escalation_resolution_summary"
EncryptedString(field_name="learned_answer"
EncryptedString(field_name="learned_question"
EncryptedString(field_name="message_body_raw"
EncryptedString(field_name="message_body_text"
EncryptedString(field_name="message_subject"
EncryptedString(field_name="response_content"
```

9/9 columns wrapped. PASS.

### Promise 2 — AES-256-GCM + per-tenant HKDF key (data-model "AES-256")

```
$ grep -nE "AESGCM|HKDF|SHA256" src/sequor/db/encrypted_column.py
20:from cryptography.hazmat.primitives.ciphers.aead import AESGCM
23:from cryptography.hazmat.primitives.hashes import SHA256
24:from cryptography.hazmat.primitives.kdf.hkdf import HKDF
64-66: hkdf = HKDF(algorithm=SHA256(), length=32, ...)   # per-field key derivation
```

PASS.

### Promise 3 (C1) — `set_tenant_key`/`bind_tenant` wired at every write/read path

```
$ grep -rn "bind_tenant\|set_tenant_context" src/sequor --include='*.py' | grep -v "def "
email/inbound.py:96, whatsapp/inbound.py:85          — inbound processors (after account resolution)
email/auto_reply.py:237/286, whatsapp/auto_reply.py:250/295  — auto-reply (1a + d3a4abd)
digest/service.py:58 (send_digest), :263 (gather_digest_data)  — C3 digest read path
ai/learning.py:149, :204                              — learning loop own-session
compliance.py:114                                     — erasure
onboarding/service.py:161                             — signup provision
onboarding/app.py:931/980/1044/1070/1133              — 5 portal endpoints
```

EscalationService is pre-bound by its only callers (auto_reply) — no own bind needed.
PASS (mechanical sweep SWEEP-2 + SWEEP-1 confirm no raw-SQL bypass on wrapped tables
other than the reconciled learning.py).

### Promise 4 (C2) — learning.py raw SQL reconciled; field_names match the ORM

```
ORM:           EncryptedString(field_name="learned_question"), (field_name="learned_answer")
learning.py:   encrypt_field(key, "learned_question"/"learned_answer"), decrypt_field(same)
```

Field_names byte-match → byte-compatible ciphertext. PASS.

### Promise 5 — Tier-2 round-trip test on real Postgres

```
$ pytest tests/integration/test_encryption_round_trip.py -q
6 passed
```

Ciphertext-at-rest, plaintext-on-read, per-tenant key isolation, fail-closed under
APP_ENV=production, learning raw-SQL round-trip, erasure production path. PASS.

## Divergences surfaced (logged, not 1b-introduced)

- **Bootstrap gap (pre-existing, out of 1b scope):** `email/inbound._resolve_account`
  filters `Account.owner_email ==`/`email_address ==` (both already EncryptedString
  since the initial schema) to resolve the tenant — equality-on-encrypted-column
  cannot match in production (random GCM nonce) and result-decryption fail-closes
  with no key. Predates 1b (Account was encrypted in the initial migration
  `5ab03308b1f3`). Fix shape: a blind index like `BackupContact.email_blind_index`.
  Disposition: journal RISK + tracked for the data-layer wave (not a 1b regression).
  **→ CLOSED by shard 1f (commit aca5672), see A1-inbound-resolve below.**

---

## Spec source: DEVIATIONS §A1 (inbound account resolution), shard 1f (commit aca5672)

Branch `feat/data-layer-security`. Shard 1f = commit `aca5672`. The journal/0013
RISK ("bootstrap gap") is the closed loop. Every row = literal command + actual output.

### Promise 1 — Account carries two email blind-index columns (mirror BackupContact)

```
$ grep -oE 'Mapped\[Optional\[str\]\] = mapped_column\(String\(64\)' src/sequor/db/models.py | wc -l
3
$ grep -nE 'owner_email_blind_index|email_address_blind_index' src/sequor/db/models.py
264:    owner_email_blind_index: Mapped[Optional[str]] = mapped_column(String(64), nullable=True)
265:    email_address_blind_index: Mapped[Optional[str]] = mapped_column(String(64), nullable=True)
288:        Index("ix_accounts_owner_email_blind_index", "owner_email_blind_index"),
289:        Index("ix_accounts_email_address_blind_index", "email_address_blind_index"),
```

Both columns + both indexes present. The third String(64) match is BackupContact.email_blind_index. PASS.

### Promise 2 — the dead ciphertext index is dropped (no new equality-on-ciphertext)

```
$ grep -nE 'ix_accounts_owner_email\b' src/sequor/db/models.py
(empty — only ix_accounts_owner_email_blind_index / ix_accounts_email_address_blind_index remain)
$ grep -nE 'drop_index.*ix_accounts_owner_email' src/sequor/db/migrations/versions/c3a9e1d4b702_add_account_email_blind_index.py
    op.drop_index("ix_accounts_owner_email", table_name="accounts")
```

Dead index removed from the model + dropped by the migration. PASS.

### Promise 3 (C1) — signup populates both Account blind indexes

```
$ grep -nE 'owner_email_blind_index=|email_address_blind_index=' src/sequor/onboarding/service.py
183:        owner_email_blind_index=email_index,
186:        email_address_blind_index=email_index,
```

Both set from `email_index` (computed at service.py:135 via `compute_email_blind_index(request.owner_email)`,
the same value already stored on BackupContact.email_blind_index). PASS.

### Promise 4 (the fix) — both resolvers look up by blind index / phone via raw projection, never materializing encrypted columns

```
$ grep -nE 'encryption_master_key|raw_execute|compute_email_blind_index' src/sequor/email/inbound.py src/sequor/whatsapp/inbound.py
src/sequor/whatsapp/inbound.py:172:        if not settings.encryption_master_key:     # dev fallback gate
src/sequor/whatsapp/inbound.py:185:        rows = await self._db.raw_execute(        # raw projection (prod)
src/sequor/email/inbound.py:337:        if not settings.encryption_master_key:      # dev fallback gate
src/sequor/email/inbound.py:349:        idx = compute_email_blind_index(to_email)   # blind index (prod)
src/sequor/email/inbound.py:350:        rows = await self._db.raw_execute(         # raw projection (prod)
$ grep -nE 'f\"SELECT|f'\''SELECT|format\(.*SELECT' src/sequor/email/inbound.py src/sequor/whatsapp/inbound.py
(empty — all resolver SQL uses :idx / :phones bound params)
```

Both resolvers branch on the master key; prod path uses raw projection with bound params (no f-string SQL). PASS.

### Promise 5 — the resolver's returned dict shape is consumable (no downstream KeyError)

```
$ grep -nE 'account\[|account\.get' src/sequor/email/inbound.py src/sequor/whatsapp/inbound.py
src/sequor/email/inbound.py:88:        tenant_id = account["tenant_id"]
src/sequor/email/inbound.py:89:        account_id = account["id"]
src/sequor/whatsapp/inbound.py:78:        tenant_id = account["tenant_id"]
src/sequor/whatsapp/inbound.py:79:        account_id = account["id"]
```

Only `id` + `tenant_id` are read — both in the raw projection (`SELECT id, tenant_id, name, status[/whatsapp_phone]`). No caller reads owner_email/email_address off the resolved dict. PASS.

### Promise 6 — `raw_execute` is not an orphan (≥1 production call site + a Tier-2 test)

```
$ grep -rn 'raw_execute' src/ tests/ --include='*.py'
src/sequor/db/crud.py:108:    async def raw_execute(                       # definition
src/sequor/email/inbound.py:350:        rows = await self._db.raw_execute(   # prod call site 1
src/sequor/whatsapp/inbound.py:185:        rows = await self._db.raw_execute(# prod call site 2
```

2 production call sites + exercised by tests/integration/test_inbound_account_resolution.py (4 tests collected). PASS.

### Promise 7 — alembic dependency declared (Declared=Imported; migrations import `from alembic import op`)

```
$ grep -nE '"alembic' pyproject.toml
38:    "alembic>=1.13.0",
```

PASS (pre-existing gap closed by 1f).

### Promise 8 — Tier-2 inbound-resolves-account under prod fail-closed (the original 500 is gone)

```
$ APP_ENV=production + master key + real PG:
$ pytest tests/integration/test_inbound_account_resolution.py -q
4 passed in 3.65s
  - test_signup_populates_account_blind_indexes
  - test_email_resolver_finds_account_under_prod_without_tenant_key   (also proves naive ORM load still raises RuntimeError)
  - test_email_resolver_returns_none_for_unknown_address
  - test_whatsapp_resolver_finds_account_by_phone_under_prod
```

PASS.

### Schema-migration Rule 1a sweep — no NEW inline DDL outside migrations in the 1f diff

```
$ git diff f72bb9e..aca5672 -- src/ | grep -E 'CREATE\s+(UNIQUE\s+)?(TABLE|INDEX|SCHEMA)|ALTER\s+(TABLE|INDEX)|DROP\s+(TABLE|SCHEMA|INDEX)'
(empty — all DDL is in c3a9e1d4b702_add_account_email_blind_index.py)
```

PASS.

## Divergences surfaced this round (1f)

- **alembic was undeclared** (pre-existing, surfaced + fixed same-shard): migration files
  imported `from alembic import op` but pyproject had no alembic dep + it was not installed.
  The test loop uses `init_db()`/`create_all` (not alembic) so this stayed hidden; the first
  `alembic upgrade` would have failed. Fixed: `pyproject.toml` now declares `alembic>=1.13.0`.
  This is a same-class (Declared=Imported) fix within shard budget, fixed immediately per
  autonomous-execution MUST-4.
