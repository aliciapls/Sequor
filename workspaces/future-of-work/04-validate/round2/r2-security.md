# r2-security.md — /redteam Round 2 Security Re-Audit (Sequor comms-wedge)

Independent read-only re-derivation against CURRENT code on `fix/redteam-r1-security-correctness`. Scope: `src/sequor/**` + `api/index.py`. Every claim quotes exact `file:line` evidence inline. Posture L5_DELEGATED. RAN: 18 files read, 9 greps executed.

## Part 1 — Closure verification of the 6 ①-defects

| ①   | Defect                 | file:line                                              | Quoted evidence (fail-closed / fix mechanism)                                                                                                                                                                                                                                                                                                                                                                                              | Verdict                                                           |
| --- | ---------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------- |
| 1   | JWT fail-closed        | `auth.py:63-70,89,95`                                  | `_signing_secret()`: `if settings.app_env == "development": ... return _DEV_ONLY_SECRET` then `raise RuntimeError("JWT_SECRET is unset...")`. Both paths route through it: `jwt.encode(..., _signing_secret(), ...)` (`:89`) and `jwt.decode(token, _signing_secret(), ...)` (`:95`). Grep: only these two JWT sites. Fail-closed anchored on `jwt_secret` presence (NOT `app_env`), so a prod deploy that forgets `APP_ENV` still raises. | **CLOSED**                                                        |
| 2   | Encryption fail-closed | `encrypted_column.py:141-146,165-170` + `config.py:18` | Both bind + result branch: `if settings.app_env != "development": raise RuntimeError(...)` then `return value  # store plaintext ONLY in development`. Zero raw `os.environ["APP_ENV"]` reads remain. **Residual:** `config.py:18` `app_env: str = "development"` — the default IS the plaintext-permitting value (see M1).                                                                                                                | **CLOSED** (divergence sub-issue) / residual fail-open on default |
| 3   | RAG `text()`           | `vector_store.py:75,128`                               | `store_chunks`: `text("""INSERT INTO document_chunks ...""")`; `search`: `text("""SELECT ... WHERE tenant_id = :tenant_id""")`. Sibling raw-SQL swept: `ingestion.py:318,369` + `learning.py:142,188,234` all `text()`+binds; `learning.py:188` f-string interpolates only the static literal `"AND account_id = :account_id"` — no user data in the SQL string.                                                                           | **CLOSED**                                                        |
| 4   | ConfidenceBadge enum   | `models.py:136-143` + migration `5ab03308b1f3:257`     | Enum `high/moderate/low/uncertain` matches migration `sa.Enum('high','moderate','low','uncertain', name='confidence_badge')`. Producers `rag_pipeline.py:222`, `response.py:164` emit `"uncertain"`.                                                                                                                                                                                                                                       | **CLOSED**                                                        |
| 5   | Error-leak handlers    | `onboarding/app.py:138,646,1840-1846,1849-1854`        | `grep format_exc → 0` across `src/`. Login/signup/upload/portal_dashboard return generic 500s. Remaining `str(e)` sites (`:135,173,219,254,268`) are `except ValueError` validation feedback (422), not tracebacks.                                                                                                                                                                                                                        | **CLOSED**                                                        |
| 6   | PDPA erasure           | `compliance.py:133-148,150-156,164-195`                | Message scrub via `update(Message)...values(subject="[erased]", body_text="[erased]", body_raw="[erased]")`; `DocumentChunk` step removed; over-deletion closed (`escalation_ids` guarded by `if message_ids:`, etc. — empty sets affect zero rows). Referenced columns exist (`Escalation.message_id`, `LearnedAnswer.source_escalation_id`).                                                                                             | **CLOSED**                                                        |

All 6 ①-defects independently re-derived as genuinely CLOSED on current code.

## Part 2 — Residual findings (re-derived from scratch)

| SEV    | Title                                                                   | file:line                                             | Quoted evidence                                                                                                                                                                                                    | Genuine-defect / residual                                          | Fix                                                                                                           |
| ------ | ----------------------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| MEDIUM | M1 — Encryption fails OPEN on shipped default `app_env`                 | `config.py:18`; `encrypted_column.py:141,146,165,170` | `app_env: str = "development"`; plaintext branch fires when `settings.app_env == "development"` (the default) AND `tenant_key is None`. Prod deploy omitting `APP_ENV` writes plaintext PII silently.              | Genuine (fail-CLOSED is conditional on explicit non-dev `app_env`) | Default `app_env="production"`, OR require explicit `APP_ENV=development` opt-in; raise otherwise             |
| MEDIUM | M2 — `NameError: settings` crashes admin backfill                       | `onboarding/app.py:679`                               | `km = KeyManager(settings.encryption_master_key)` — `settings` never imported in `backfill_blind_indexes` (imports `:658-663`), not module-scope. 500s on every call.                                              | Genuine defect (zero-tolerance Rule 1)                             | Add `from sequor.config import settings` in handler                                                           |
| MEDIUM | M3 — `NameError: select` crashes keyphrase create                       | `onboarding/app.py:1400-1401`                         | `portal_api_keyphrase_create` imports (`:1379-1382`) omit `select`; `:1400` `select(Document).where(...)` → `NameError` on every valid POST.                                                                       | Genuine defect                                                     | Add `select` to the local `from sqlalchemy import ...`                                                        |
| MEDIUM | M4 — Cross-tenant admin op + ID/exception leak                          | `onboarding/app.py:650-704`                           | `_require_auth` only (no `role=="admin"`); iterates ALL tenants' `BackupContact` (`:671-674`); `errors.append(f"{contact.id}: {e}")` (`:695`) returns cross-tenant IDs + raw exception text (`:699-704`).          | Residual (design)                                                  | Gate on `role=="admin"`, scope to caller tenant, drop raw `{e}`                                               |
| MEDIUM | M5 — Unauthenticated DNS lookup oracle                                  | `onboarding/app.py:225-231,234-240`; `dns/service.py` | `/api/v1/dns/records` + `/verify` take attacker `domain`, no auth/rate-limit; `verify_dns_records(domain)` → server-side DNS resolution → amplifier/rebinding-recon.                                               | Residual (design)                                                  | Authenticate, rate-limit, constrain to caller's domains                                                       |
| MEDIUM | M6 — Onboarding upload reads whole file before size check               | `onboarding/app.py:185`                               | `content = await file.read()` no size cap; portal has 25MB check, this path none.                                                                                                                                  | Residual                                                           | Stream + cap before read (mirror portal 25MB)                                                                 |
| MEDIUM | M7 — Rate limiter fails OPEN at capacity + serverless-ineffective       | `onboarding/rate_limiter.py:45-47`                    | `if len(self._windows) >= _MAX_TRACKED_KEYS: ...; return True` — 10k keys → every NEW key allowed. In-memory per-process → ineffective on serverless.                                                              | Residual                                                           | Shared store (Redis); evict oldest rather than allow-on-full                                                  |
| MEDIUM | M8 — SendGrid webhook verification disabled by default + fragile        | `email/inbound.py:53-63`; `onboarding/app.py:283,307` | `if settings.app_env != "development":` gates the whole verification block — default skips it. Verifies only when both non-None; lossy `decode(errors="replace")` corrupts bytes; no timestamp/replay.             | Residual (design)                                                  | Verify exact raw bytes incl. timestamp; reject stale; hard-reject missing raw_body; don't gate on dev default |
| MEDIUM | M9 — Prompt injection into classifier + RAG auto-send                   | `ai/classifier.py`, `ai/rag_pipeline.py`              | Customer message + passages interpolated with no instruction/data separation; crafted "confidence 1.0" can bias self-score toward the 0.90 auto-send gate. Bounded: no tool use, tenant-scoped, same-sender reply. | Residual (design)                                                  | Instruction/data separation + out-of-band injection check                                                     |
| LOW    | L1 — Portal shells gate on cookie presence; dead+broken `_portal_guard` | `onboarding/app.py:1784,1836,1813-1826`               | `if not token:` presence-only renders shells for any non-empty cookie. `_portal_guard` (`:1813`) unreachable dead code, broken body (`sessionStorage` undefined-name). Data endpoints DO validate.                 | Genuine (dead/stub code, zero-tolerance Rule 2) + residual         | Validate token in page guards; delete `_portal_guard`                                                         |
| LOW    | L2 — Login timing oracle → email enumeration                            | `onboarding/app.py:565-568,576`                       | `if not contact: return 401` short-circuits BEFORE `verify_password` (`:576`) — absent-email faster despite uniform 401.                                                                                           | Residual                                                           | Dummy bcrypt compare on no-contact path                                                                       |
| LOW    | L3 — Deterministic global email blind index → cross-tenant correlation  | `encrypted_column.py:79-104`                          | `compute_email_blind_index` derives one global key (`info=b"sequor-global-email-lookup"`) → same email → same index across ALL tenants.                                                                            | Residual (documented)                                              | Accept as documented residual, or per-tenant-salt                                                             |

## Part 3 — Carried deferred ②-scope (design-residual HIGH — NOT new)

| SEV             | Title                                                                        | file:line              | Evidence                                                                                                                   | Disposition                                                           |
| --------------- | ---------------------------------------------------------------------------- | ---------------------- | -------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| HIGH (deferred) | Message/Response/chunk/learned content PLAINTEXT vs spec "all PII encrypted" | `models.py:414-416`    | `subject: String(500)`, `body_text: Text`, `body_raw: Text` plaintext; only identifiers use `EncryptedString`.             | ②-scope: build encryption OR amend spec w/ sign-off                   |
| HIGH (deferred) | Schema-per-tenant isolation not enforced (column-based)                      | all `/api/v1/portal/*` | Every portal handler filters by `tenant_id` column; `session_for_tenant` unused. Spec mandates schema-per-tenant for PDPA. | ②-scope: route via `session_for_tenant` OR amend spec + PDPA sign-off |

## Part 4 — Per-surface verdicts

- **auth**: PASS — dev-secret fallback fail-closed on `jwt_secret` presence, independent of `app_env`; both sign+verify route through `_signing_secret()`. Residual: no `aud`/`iss`, `role` largely unused for authz (M4).
- **encryption**: PASS on the divergence fix; **residual fail-open on default `app_env`** (M1).
- **injection (SQL)**: PASS — parameterized throughout incl. all `ai/` raw-SQL.
- **injection (prompt)**: PARTIAL — no separation on auto-send path (M9).
- **error-handling**: PASS — 0 `format_exc`; generic 500s at all sites.
- **PDPA erasure**: PASS — scrub added, over-deletion structurally impossible. Note: scrubs to `"[erased]"` rather than hard-deleting `Message` rows (spec says "Hard delete") — design deviation.
- **correctness (latent NameErrors)**: FAIL — `app.py:679` + `app.py:1400` crash their endpoints (zero-tolerance Rule 1).
- **rate-limit**: PARTIAL — fails OPEN at capacity + serverless-ineffective (M7).
- **webhook-sig**: PARTIAL — WhatsApp/Meta HMAC + Stripe correct; SendGrid disabled by default + lossy decode (M8).
- **secrets**: PASS — no hardcoded keys; logs masked.
- **tenant-isolation**: DEFERRED (②-scope).

## Counts

- ①-defects: **6/6 CLOSED** (①-2 with a MEDIUM residual, M1).
- New CRITICAL: **0**. New HIGH: **0**.
- Residual: **9 MEDIUM** (M2/M3 = genuine correctness defects; M1/M4-M9 = residuals), **3 LOW** (L1 = dead/stub code).
- Carried deferred ②-scope: **2 HIGH design-residuals** (not new).

**Convergence:** the ①-defect subset is fully closed. Full `/redteam` convergence remains blocked by (a) the two NameError defects (M2/M3) + M1 fail-open default + L1 dead code (all within-shard mechanical fixes under zero-tolerance), and (b) the ②-scope decision (message-content encryption + schema-per-tenant).
